import React from 'react';
import Title from '../../Title';

const About = () => {
  return (
    <section id='about'>
      <Title word={['a', 'b', 'o', 'u', 't']} y={0.96} />
    </section>
  );
};

export default About;
