import React from 'react';
import Title from '../../Title';

const Dev = () => {
  return (
    <section id='dev'>
      <Title word={['d', 'e', 'v']} y={0.32} start={0.23} />
    </section>
  );
};

export default Dev;
