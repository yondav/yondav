import React from 'react';
import Title from '../../Title';

const Dev = () => {
  return (
    <section id='dev'>
      <Title word={['d', 'e', 'v']} y={0.29} start={0.24} />
    </section>
  );
};

export default Dev;
